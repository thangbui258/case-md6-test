import NavBar from "../NavBar";

const NavBarTransactions = () => {
    const handleClick = ()=>{

    }
    return (
        <div>
            <NavBar>

                <button onClick={handleClick}>tim kiem</button>
                day laf transactions
            </NavBar>
        </div>
    );
};

export default NavBarTransactions;